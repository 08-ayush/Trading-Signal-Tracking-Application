# Local: default in dev is http://localhost:4000/api if you omit this.
# Vercel / production: required unless you proxy /api on the same host — set to your live API root, e.g.:
# VITE_API_BASE_URL=https://your-backend.railway.app/api
VITE_API_BASE_URL=http://localhost:4000/api
